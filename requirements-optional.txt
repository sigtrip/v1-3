# Optional dependencies for extended Argos features
# Install only when corresponding hardware/integrations are needed:
#   pip install -r requirements-optional.txt

# ── SDR / RF reconnaissance (AirSnitch) ───────────────────
pyrtlsdr>=0.2.93
# SoapySDR Python bindings are not reliably published on PyPI for all platforms
# (especially Windows). Install SoapySDR separately via system package manager
# and vendor drivers if you need extended SDR backends.
# Example:
#   Windows: install SoapySDR + drivers manually (or via vendor installer)
#   Linux:   sudo apt install soapysdr-tools python3-soapysdr

# ── Wireless analysis (WiFi Sentinel) ─────────────────────
scapy>=2.5.0

# ── Scientific/processing helpers ─────────────────────────
numpy>=1.26.0

# ── Realtime voice pipeline helpers (Pipecat) ─────────────
pipecat-ai[silero]

# ── NFC / SmartCard (NFC Manager) ─────────────────────────
nfcpy>=1.0.4
pyscard>=2.0.10

# ── Bluetooth stack extras ────────────────────────────────
bleak>=0.22.3

# ── Enterprise AI bridge (Watsonx) ────────────────────────
ibm-watsonx-ai>=1.1.8

# ── IBM Quantum Runtime bridge (optional) ─────────────────
qiskit>=1.1.0
qiskit-ibm-runtime>=0.32.0

# ── Optional mobile build toolchain helper ────────────────
# buildozer is typically installed separately in Android build env
# buildozer>=1.5.0
